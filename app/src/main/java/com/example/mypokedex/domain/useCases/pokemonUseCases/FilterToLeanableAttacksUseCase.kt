package com.example.mypokedex.domain.useCases.pokemonUseCases

import com.example.mypokedex.core.Constantes.METODO_DE_APRENDIZAGEM
import com.example.mypokedex.domain.model.pokemonMove.PokemonMoves

class FilterToLeanableAttacksUseCase {
    //    fun filterToLearnableAttacks(pokemonMoves: List<PokemonMoves>): List<PokemonMoves> =
//        pokemonMoves.filter { it.moveDetails.last().learnMethod.method == Constantes.METODO_DE_APRENDIZAGEM }
//            .sortedBy { it.moveDetails.last().levelLearned }
//    operator fun invoke(pokemonMoves: List<PokemonMoves>): List<PokemonMoves> {
////        val oi = pokemonMoves.filter { it.moveDetails.map { it.learnMethod.method }.eac == METODO_DE_APRENDIZAGEM}
////        pokemonMoves.filter { it.moveDetails.map { it.learnMethod } == METODO_DE_APRENDIZAGEM }
//    }
}